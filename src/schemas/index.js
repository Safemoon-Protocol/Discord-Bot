const requiredString = {
  type: String,
  required: true
}

const requiredNumber = {
  type: Number,
  required: true
}

const requiredBool = {
  type: Boolean,
  required: true
}

module.exports = {
  requiredString,
  requiredNumber,
  requiredBool,
}
